---
description: Remove the Promptcellar plugin entry from Claude Code (captured .prompts/ data is left intact)
allowed-tools: ["Bash"]
---

Run `pc-cli uninstall` via the Bash tool and show the output verbatim.
