---
description: Disable Promptcellar capture (default scope is repo-level / committed; use --for-me for personal opt-out or --global for the machine-wide kill-switch)
argument-hint: "[--for-me] [--global]"
allowed-tools: ["Bash"]
---

Run `pc-cli disable $ARGUMENTS` via the Bash tool and show the output verbatim. If the layer is "team", repeat the reminder about committing + pushing.
