---
description: Show Promptcellar capture status for this repo (resolved config, paths, record counts)
allowed-tools: ["Bash"]
---

Run `pc-cli status` via the Bash tool and show the output verbatim.
