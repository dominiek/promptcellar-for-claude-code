---
description: Diagnose Promptcellar — check hook binaries, plugin wiring, resolved config
allowed-tools: ["Bash"]
---

Run `pc-cli doctor` via the Bash tool and show the output verbatim. If any check fails (✗), explain what the user should do to fix it.
