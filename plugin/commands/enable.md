---
description: Enable Promptcellar capture (default scope is repo-level / committed; use --for-me for personal opt-in or --global for the machine-wide toggle)
argument-hint: "[--for-me] [--global]"
allowed-tools: ["Bash"]
---

Run `pc-cli enable $ARGUMENTS` via the Bash tool and show the output verbatim. If the layer is "team", the printed reminder to commit + push the config change is important — repeat it.
