---
description: Discovery-plugin self-test. Exercises the plugin-shipped slash command path so we can capture command_source/expansion_type values via the hook dumps.
---

Reply with exactly the single word "pong" and nothing else.
